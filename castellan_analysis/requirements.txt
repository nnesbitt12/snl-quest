# Python dependencies for the Castellan Farm BESS analysis workflow.
# Install with:  pip install -r castellan_analysis/requirements.txt
#
# NOTE: the QuESt dispatch engine also needs the GLPK solver *binary* on PATH:
#   - Debian/Ubuntu: sudo apt-get install glpk-utils
#   - macOS:         brew install glpk
#   - conda:         conda install -c conda-forge glpk
# Without a solver the workflow automatically falls back to the (approximate)
# built-in heuristic dispatch engine.

pandas>=2.0
numpy>=1.24
openpyxl>=3.1        # read the .xlsx uploads
pyomo>=6.6           # QuESt BtmOptimizer (linear program)
holidays>=0.40       # QuESt rate schedule (weekend/holiday handling)
pyutilib>=6.0        # required by QuESt's optimizer base class
tabulate>=0.9        # markdown tables in the report
six>=1.16            # required by QuESt's optimizer base class
